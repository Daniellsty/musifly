import React from 'react'
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Offline",
};

const page = () => {
  return (
    <div>about page</div>
  )
}

export default page